[workspace]
resolver = "2"

members = [
    "surveyor-gnc",
    "surveyor-graphics",
    "surveyor-physics",
    "surveyor-app",
]

# Enable max optimizations for dependencies, but not for our code:
[profile.dev.package."*"]
opt-level = 3
